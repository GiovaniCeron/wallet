const Conta = require('../model/ContaModel');

module.exports = {
       
    /**
     * Cadastra uma conta
     * @param request 
     * @param response 
     */
    async insert(request, response) {
    
    }
}